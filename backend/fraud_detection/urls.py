from django.urls import path

app_name = 'fraud_detection'

urlpatterns = [
    # Fraud detection is handled internally, no direct endpoints for MVP
    # Future: Add ML model training and prediction endpoints
]
